export type TRegister = {
    name: string;
    email: string;
    password: string;
}
export type TAuthUser = {
    id: string;
    email: string;
    name: string;
}

export type TLogin = {
    email: string;
    password: string;
}
